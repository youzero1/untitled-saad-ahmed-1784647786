export default function WeatherPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-yellow-100">
      <p className="text-red-500 text-2xl font-semibold">Weather Forecast — Loading...</p>
    </div>
  );
}
